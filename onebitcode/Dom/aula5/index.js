function register(element){
    const username = element.children.username.value
    const password = element.children.password.value
    const passwordConfimaition = element.children.passwordConfimaition.value

    console.log({username,password,passwordConfimaition})

}